development:
  sessions:
    default:
      database: git-notifier
      hosts:
        - localhost:27017
production:
  sessions:
    default:
      database: git-notifier
      uri:
        - <%= ENV["MONGOLAB_URI"] %>

